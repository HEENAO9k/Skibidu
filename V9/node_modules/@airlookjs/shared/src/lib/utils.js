export const parseIntEnv = (env, defaultValue) => {
    if (env === undefined) {
        return defaultValue;
    }
    const parsed = parseInt(env);
    if (isNaN(parsed)) {
        return defaultValue;
    }
    return parsed;
};
export const parseFloatEnv = (env, defaultValue) => {
    if (env === undefined) {
        return defaultValue;
    }
    const parsed = parseFloat(env);
    if (isNaN(parsed)) {
        return defaultValue;
    }
    return parsed;
};
export const parseBoolEnv = (env, defaultValue) => {
    if (env === undefined) {
        return defaultValue;
    }
    if (env === 'true') {
        return true;
    }
    if (env === 'false') {
        return false;
    }
    return defaultValue;
};
//# sourceMappingURL=utils.js.map