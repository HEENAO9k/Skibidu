import url from 'node:url';
export const stringIsAValidUrl = (s, protocols) => {
    try {
        new url.URL(s);
        const parsed = url.parse(s);
        return protocols
            ? parsed.protocol
                ? protocols.map((x) => `${x.toLowerCase()}:`).includes(parsed.protocol)
                : false
            : true;
    }
    catch (_err) {
        return false;
    }
};
//# sourceMappingURL=validateUrl.js.map