import { type ScenesOutput } from './scenedetect.js';
import { ShareInfo } from '@airlookjs/shared';
import type { FastifyPluginCallback } from 'fastify';
export interface ScenedetectDataResponse extends ScenesOutput {
    cached: boolean;
    version: string;
    cachedVersion?: string;
}
export interface ScenedetectRoutesOptions {
    prefix: string;
    shares: ShareInfo[];
    cacheDir: string;
}
export declare const routes: FastifyPluginCallback<ScenedetectRoutesOptions>;
//# sourceMappingURL=routes.d.ts.map